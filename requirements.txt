# Core
numpy>=1.23
pandas>=1.5
PyYAML>=6.0

# ML / preprocessing
scikit-learn>=1.2
scipy>=1.9
joblib>=1.2

# Parquet IO (the Census downloader and other scripts use PyArrow datasets)
pyarrow>=16.0
fastparquet>=2023.8.0

# Gradient boosting
lightgbm>=4.0

# Distance correlation (Python 3.9 compatible; fairness_env uses 0.6)
dcor>=0.6,<0.7

# Plotting / viz
matplotlib>=3.6
seaborn>=0.12
plotly>=5.18
shapely>=2.0

# Optimization (stacking_pf_opt)
cvxpy>=1.4
ecos>=2.0
scs>=3.2

# Optional (faster / commercial solver)
# mosek>=10.0
